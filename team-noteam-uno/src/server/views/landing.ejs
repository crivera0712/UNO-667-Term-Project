<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            min-height: 100vh;
            background: linear-gradient(135deg, #1a1a1a, #2a2a2a);
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
        }

        .menu-container {
            background: rgba(255, 255, 255, 0.1);
            padding: 2rem;
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            text-align: center;
            backdrop-filter: blur(10px);
            width: 80%;
            max-width: 500px;
        }

        .game-title {
            font-size: 3.5rem;
            color: #ff4444;
            margin-bottom: 2rem;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }

        .welcome-text {
            font-size: 1.2rem;
            margin-bottom: 2rem;
        }

        .menu-button {
            display: block;
            width: 80%;
            margin: 1rem auto;
            padding: 1rem;
            font-size: 1.2rem;
            border: none;
            border-radius: 8px;
            background: #ff4444;
            color: white;
            cursor: pointer;
            transition: transform 0.2s, background 0.2s;
        }

        .menu-button:hover {
            transform: translateY(-2px);
            background: #ff6666;
        }

        .menu-button.secondary {
            background: #444;
        }

        .menu-button.secondary:hover {
            background: #555;
        }
    </style>
</head>
<body>
    <div class="menu-container">
        <h1 class="game-title">UNO</h1>
        <p class="welcome-text">Welcome, <%= username %>!</p>

        <a href="/play" class="menu-button">Play Game</a>
        <a href="/leaderboard" class="menu-button">Leaderboard</a>
        <% if (!username || username === "Player") { %>
            <a href="/login" class="menu-button secondary">Login</a>
            <a href="/register" class="menu-button secondary">Register</a>
        <% } else { %>
            <a href="/profile" class="menu-button secondary">Profile</a>
            <a href="/logout" class="menu-button secondary">Logout</a>
        <% } %>
    </div>
</body>
</html>
